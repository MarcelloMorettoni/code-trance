```
x^2 + 5x + 8 = 0
```
```
$: n("8 4 2 2 4 8")
  .scale("B:major")   // We map the numbers to the C Minor scale
  .s("piano")
  .velocity("0.6 0.5 0.4 0.4 0.5 0.8") // Dynamics follow the curve too (softer at the bottom)
  .room(0.5)          // Add some room reverb for the piano
  ._pianoroll()
```
